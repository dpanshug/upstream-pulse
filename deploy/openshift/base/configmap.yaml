apiVersion: v1
kind: ConfigMap
metadata:
  name: upstream-pulse-config
  namespace: upstream-pulse
  labels:
    app.kubernetes.io/part-of: upstream-pulse
data:
  NODE_ENV: "production"
  PORT: "3000"
  LOG_LEVEL: "info"
  PROMETHEUS_ENABLED: "true"
  GITHUB_TEAM_ORG: "opendatahub-io"
  ADMIN_USERS: "admin"
  ADMIN_GROUPS: ""
