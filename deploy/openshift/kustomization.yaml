# Backward-compatible redirect — can be removed once all Argo CD Applications
# point to deploy/openshift/overlays/<env> instead of deploy/openshift.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- overlays/dev
images:
- name: quay.io/org-pulse/upstream-pulse-backend
  newTag: 9685c87
- name: quay.io/org-pulse/upstream-pulse-frontend
  newTag: 9685c87
